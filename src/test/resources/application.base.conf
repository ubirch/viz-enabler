vizEnabler {

  elasticSearch {
    version = "6.7.0"
    protocol = "http"
    host = "localhost"
    port = 9201
    io_user = "not needed"
    io_password = "not needed"

    deviceData {
      index = "device_data_vizualizer"
    }

  }
}
